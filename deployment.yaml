apiVersion: apps/v1

kind: Deployment

metadata:
  name: flask-deployment

spec:
  replicas: 2

  selector:
    matchLabels:
      app: flask-app

  template:

    metadata:
      labels:
        app: flask-app

    spec:
      containers:
        - name: flask-container
          image: notes_web_app-flask-app:latest
          imagePullPolicy: IfNotPresent

          env:
            - name: DB_HOST
              value: "mysql-db"

          ports:
            - containerPort: 5000